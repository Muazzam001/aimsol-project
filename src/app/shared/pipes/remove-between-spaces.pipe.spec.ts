import { RemoveBetweenSpacesPipe } from './remove-between-spaces.pipe';

describe('RemoveBetweenSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveBetweenSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
